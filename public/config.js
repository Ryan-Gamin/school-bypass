let _CONFIG = {
    // This points the proxy to a working public backend
    bare: 'https://tomp.app/',
    
    // These are standard settings to keep it running smoothly
    prefix: '/service/',
    encodeUrl: x => x,
    decodeUrl: x => x,
};
